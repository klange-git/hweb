Local
=====
cd path/to/repo
python -m venv ./.venv
. ./.venv/bin/activate
pip install --upgrade pip
pip install -r requirements.txt


Docker
======
docker build --tag hweb .
docker run -d -p 5000:5000 hweb

